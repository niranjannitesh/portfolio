# theux.dev

Personal site. Astro, Tailwind, static output.

```
bun install
bun run dev      # http://localhost:4321
bun run build    # -> dist/
bun run check
```

Content lives in `src/data/` — `projects.ts`, `notes.ts`, `tools.ts`.
